/** Version du jeu, lue dans package.json au build (vite.config.ts). */
declare const __VERSION_JEU__: string;
/** Commit du build, en 7 caractères, ou « dev » (vite.config.ts). */
declare const __BUILD__: string;
